# Synthetic legacy skill

This inert fixture represents an old run without a versioned run descriptor or complete
root manifests. It is test data only, does not represent a real creator, and must never be
treated as verified or ready for use.
